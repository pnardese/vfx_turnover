Export subcaps file using vfx-turnover.

Run `vfx-turnover -s` to export the subcaps file. Confirm overwrite if output file already exists. Output is saved as `<stem>_subcaps.txt` next to the source EDL.
